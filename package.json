{
  "name": "compare-data-structure",
  "version": "0.0.1",
  "description": "Recursively compares a reference data structure to a given data structure to help sanity check data handling code.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jasmine"
  },
  "author": "Reid Williams",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.11.1",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "jasmine": "^2.4.1"
  }
}
